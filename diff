#!/usr/bin/php
<?php

require('vendor/autoload.php');

use \JustCoded\DotenvSync\DotenvDiff;

$argv = $_SERVER['argv'];

switch(count($argv))
{
	case 1:
		$dotenvDiff = new DotenvDiff();
		break;
	case 3:
		$dotenvDiff = new DotenvDiff($argv[1], $argv[2]);
		break;
}

echo $dotenvDiff->diff()->output();

exit($dotenvDiff->isSuccess() ? 0 : 1);

